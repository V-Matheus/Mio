{
  "name": "@mio/graphql-schema",
  "version": "0.1.0",
  "private": true,
  "description": "SDL do schema GraphQL do API Gateway — fonte única consumida pelo codegen do Web.",
  "files": [
    "schema.gql"
  ]
}
